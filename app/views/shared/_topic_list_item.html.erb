<li class="list-group-item d-flex justify-content-between align-items-start col-sm-6">
    <div class="ms-2 me-auto">
        <div class="fw-bold"><%= topic.title %></div>
        <div><%= Article.topic_articles(topic.id).count %> Articles</div>
    </div>
    <span><%= link_to "Go", topic, class:"btn btn-primary" %></span>
</li>